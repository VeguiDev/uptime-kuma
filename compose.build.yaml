services:
  uptime-kuma:
    build:
      context: .
      dockerfile: docker/dockerfile-local
      target: release
    image: uptime-kuma-local:2
    restart: unless-stopped
    shm_size: "1gb"
    volumes:
      - ./data:/app/data
